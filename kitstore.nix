{
  use = [
    { node = "@repo:glamour"; mount = "glamour"; }
    { node = "@repo:glow"; mount = "glow"; }
    { node = "@repo:lipgloss"; mount = "lipgloss"; }
    { node = "@repo:alecthomas-chroma"; mount = "chroma"; context = "Chroma syntax-highlighting implementation used by Blizzaga; inspect lexer and token contracts here."; }
  ];
}
